import {EthAddressValidatorDirective} from './eth-address-validator.directive';

describe('EthAddressValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EthAddressValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
